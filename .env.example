# Environment variables for local development
# Copy this file to .env and fill in your values

# GitHub Personal Access Token for AI chat functionality
# Generate one at: https://github.com/settings/tokens
# Requires no special scopes - just basic access
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: Override default port (defaults to 3000)
# PORT=3000

# Authorization: User role mapping for CF_Authorization header
# JSON format mapping user identifiers to roles (editor|viewer)
# Example: {"admin": "editor", "guest": "viewer", "user1": "editor"}
# USER_ROLE_MAPPING={"admin": "editor", "guest": "viewer"}